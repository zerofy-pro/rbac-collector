package constants

const (
	// EnvLogFormat is the environment variable key for setting the log format.
	EnvLogFormat = "LOG_FORMAT"

	// LogFormatConsole is the value for human-readable console logging.
	LogFormatConsole = "console"

	// LogFormatJSON is the value for machine-readable JSON logging.
	LogFormatJSON = "json"
)

const (

	// EnvCollectionIntervalSeconds is the env var for the collection interval.
	EnvCollectionIntervalSeconds = "COLLECTION_INTERVAL_SECONDS"

	// DefaultCollectionIntervalSeconds is the default collection interval.
	DefaultCollectionIntervalSeconds = "60"
)
